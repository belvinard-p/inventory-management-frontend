export const UsersCacheKeys =  Object.freeze({
    Users: 'users',
    Roles: 'roles'
} as const)

export const CompaniesCacheKeys = Object.freeze({
    Companies: 'companies'
} as const)

export const CategoriesCacheKeys = Object.freeze({
    Categories: 'categories',
    Category: 'category'
} as const)

export const ArticlesCacheKeys = Object.freeze({
    Articles: 'articles',
    Article: 'article'
} as const)

export const ClientsCacheKeys = Object.freeze({
    Clients: 'clients',
    Client: 'client'
} as const)

export const ClientOrdersCacheKeys = Object.freeze({
    ClientOrders: 'client-orders',
    ClientOrder: 'client-order'
} as const)

export const OrderClientLinesCacheKeys = Object.freeze({
    OrderClientLines: 'order-client-lines',
    OrderClientLine: 'order-client-line'
} as const)